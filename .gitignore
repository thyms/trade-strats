# Secrets
.env
.env.local

# Local config (the .example.yaml is the source of truth; copy to config.yaml)
config/config.yaml
config/config.local.yaml

# Runtime data & reports (see PRD §12.5)
data/trades.db
data/trades.db-journal
data/trades.db-wal
data/trades.db-shm
data/events.jsonl
data/events.jsonl.*

# Per-session daily reports (high churn; keep local).
# Research outputs under reports/backtest/ and reports/walk-forward/ ARE tracked.
reports/daily/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.pyright/
htmlcov/
.coverage
.coverage.*
coverage.xml
*.egg-info/
build/
dist/

# uv
.uv-cache/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
*~
